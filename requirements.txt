pandas>=2.0
numpy>=2.5.1
requests>=2.31
lxml>=4.9
html5lib>=1.1
# Indices globais / BTC / Ibovespa a vista (scripts/fetch_markets.py). O yfinance
# cuida do handshake de cookie/crumb do Yahoo; faixa aberta no patch para receber
# correcoes quando o Yahoo mexer no endpoint, travada no minor.
yfinance>=0.2.50,<0.3
